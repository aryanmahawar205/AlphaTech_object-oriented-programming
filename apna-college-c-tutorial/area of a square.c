#include <stdio.h>

int main()
{
    int side;
    printf("Enter the side of the square - ");
    scanf("%d", &side);
    printf("The area of the square is - %d square units \n", side*side);
    return 0;
}